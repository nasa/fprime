
class ${name}(SerializableType):


    def __init__(self):
    
        m_list = [
#for ($membername,$type,$format_string) in $mem_list:
            ("$membername",$type,"$format_string"),
#end for
        ]
        
        super(${name},self).__init__("${name}",m_list)
                    

