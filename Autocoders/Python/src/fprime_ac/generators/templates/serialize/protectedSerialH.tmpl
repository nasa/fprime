protected:

    enum {
        TYPE_ID = ${uuid} //!< type id
    };

#for (name,type,array_length,size,format,comment,typeinfo) in $members:
#if $array_length == None:
    ${type} m_${name}; //<! ${name} - ${comment}
#else
    ${type} m_${name}[${array_length}]; //<! ${name} - ${comment}

#end if
#end for
