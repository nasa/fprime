# Created on ${date}
# Author: ${user}
#echo "#\n"
# THIS FILE IS AUTOMATICALLY GENERATED - DO NOT EDIT!!!
#echo "#\n"
# Component Source: ${source}

# SYNC: "${sync}"
# FULL: "${full}"
# PRIORITY: "${priority}"
# COMPONENT: "${component_string}"
COMMAND ${target_caps} ${cmd_name} ${endianness} "${cmd_desc}"
#raw    <%=#end raw render "_${target_lower}_cmds_hdr.txt", locals: {id: ${opcode}} %>
    #for $name, $desc, $types, $bit_offset, $bits, $type, $min_val, $max_val, $default in $cmd_items
    #if $bit_offset < 0
    PARAMETER $name $bit_offset $bits $type $min_val $max_val $default "$desc"
      #for $type in $types
        STATE $type[0] $type[1]
      #end for
    #else
    APPEND_PARAMETER $name $bits $type $min_val $max_val $default "$desc"
      #for $type in $types
        STATE $type[0] $type[1]
      #end for
    #end if
    #end for

