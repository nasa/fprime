"""
test_pytest.py:

Checks that the pytest setup runs as expected. Done as a most-basic example of pytest to ensure the
system is setup as expected.

@author mstarch
"""
def test_pytest():
    """
    Tests that PyTest works. Why? As an example, clearly.
    """
    assert True, "PyTest works"
