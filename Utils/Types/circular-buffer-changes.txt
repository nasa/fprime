Change
Reason

Revise implementation to use a head index and an allocated size instead of head and tail pointers
Simplifies the implementation. Eliminates the one-byte discrepancy between the physical and logical store sizes.

Replace <code>get_remaining_size(bool)</code> with <code>get_allocated_size</code> and <code>get_free_size</code>
The interface is easier to understand.

Add <code>const</code> to interface functions where possible
C++ coding practice

Add SDD
Required documentation
