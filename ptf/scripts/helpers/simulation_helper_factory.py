import os

class SimulationHelperFactory:
    def getSimulationHelper(self, type = os.environ["SIMULATION"]):
        # there is probably a more anonymous way to do this...
        
        return None

        